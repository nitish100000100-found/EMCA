import { calculatorTool } from "./calculator";
import { weatherTool } from "./weather";
import { generateImageTool } from "./imageGen";
import { imageInfoTool } from "./imageInfo";

export const allTools = [calculatorTool, weatherTool, generateImageTool, imageInfoTool];

export { calculatorTool, weatherTool, generateImageTool, imageInfoTool };
